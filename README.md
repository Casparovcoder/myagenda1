# MY AIGENDA

Slimme agenda-assistent die afspraken en planningen beheert.

## Endpoints
- `POST /create-event` – maak een nieuw event aan  
- `GET /list-events` – haal een lijst van events op

## Lokale test
```bash
pip install -r requirements.txt
python app.py
